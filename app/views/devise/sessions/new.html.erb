<h2 class="top"><%= t('views.messages.login') %></h2>
<div class="test_user">
    <%= form_with(model: @user, local: true, url: session_path(resource_name)) do |f| %>
      <%= f.hidden_field :email, :value => "user-test@g.com" %>
      <%= f.hidden_field :password, :value => 111111 %>
      <button type="submit" class="testuser_btn"><%= t('views.messages.test_login') %></button>
    <% end %>
</div>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
<div class="user_form">
  <form>
      <div class="form-group">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :password %>
        <%= f.password_field :password, autocomplete: "new-password", class: 'form-control' %>
          <% if @minimum_password_length %>
            <br />
            <em><%= @minimum_password_length %> characters minimum</em>
          <% end %>
      </div>
      <!-- <div class="form-group"> -->
        <%#= form.label :password_confirmation %>
        <%#= form.password_field :password_confirmation, autocomplete: "new-password", :class => 'form-control' %>
      <!-- </div> -->
      <!-- <br> -->
      <% if devise_mapping.rememberable? %>
        <div class="form-group">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>
      <div class="login">
        <div class="login_btn">
          <button type="submit" class="normal_btn"><%= t('views.messages.login') %></button>
        </div>
        <div class="login_btn">
          <%= link_to user_twitter_omniauth_authorize_path, class: "twitter_btn" do %>
            <i class="fab fa-twitter"></i><%= t('views.messages.twitter') %>
          <% end %>
        </div>
        <div class="login_btn">
          <%= link_to t('views.messages.new'), new_user_registration_path, class: "new_btn" %>
        </div>

        </div>
      </div>
  </form>
</div>

<%#= render "devise/shared/links" %>
<% end %>
